You are formulating a single web-search query for one sub-question.

# Sub-question
{subquestion}

# Output format
Return ONLY a JSON object:

```json
{{"query": "the search query string"}}
```

The query should be 4–10 words, no quotation marks unless required.
